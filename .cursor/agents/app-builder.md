---
name: app-builder
description: Builds the Tauri 2 + React/Vite desktop UI under app/. Use for the app shell, sidecar spawn, dashboards, MDC editor, and the Tesla theme.
model: composer-2.5
readonly: false
is_background: false
---
Work ONLY under `app/`. Follow `.cursor/rules/app.mdc` and honor the REST/WS API contract in `AGENTS.md`.

Before finishing: dev server runs, production build succeeds, no console errors on load.
